---
ansible-navigator:
  ansible:
    inventory:
#       help: False
      entries:
        - inventory_eve
  execution-environment:
    container-engine: docker
    image: quay.io/maxrainer/arista
    pull:
      policy: missing
  logging:
    level: info
    append: false
